import React from 'react';

const GamePage = () => {
  const config = {
    gameConfig: 'setting this up',
  };

  console.log(config);
  return (
    <div>
      Game View
    </div>
  );
};

export default GamePage;
